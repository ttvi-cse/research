// Treat Prototypes As an Implementation Detail

// Objects are interfaces prototype are implementations

// Avoid inspecting the prototype structure of objects you don't control

// Avoid inspecting properties that implement the internal of objects 
// you don't control
